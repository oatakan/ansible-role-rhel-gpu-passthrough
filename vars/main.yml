---


cpu_type: "{{ ansible_processor[1] | lower }}"
iommu_args: "--args {{ cpu_type }}_iommu=on --args iommu=pt"
console_arg: " --args consoleblank={{ consoleblank_timeout }}"
kernel_args: "{{ iommu_args }}{{ console_arg if set_consoleblank else '' }}"